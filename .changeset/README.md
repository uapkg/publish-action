# This directory stores changeset files used by @changesets/cli.

Create a changeset when behavior, APIs, or operational semantics change:

  npm run changeset

Changesets are consumed by the release workflow to open or update a release PR,
and merged release PRs trigger automated tag/release publishing for this action.
